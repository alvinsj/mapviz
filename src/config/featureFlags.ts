export const BBOX_ZOOM = {
  key: 'feat-bbox-zoom',
  defaultValue: true,
}

export const SHOW_REGIONS = {
  key: 'feat-show-regions',
  defaultValue: true,
}

export const FLAGS = [BBOX_ZOOM, SHOW_REGIONS]

export default FLAGS
